# Local stack: run `npx supabase start` and copy the printed API URL + anon key.
# Cloud: use your project's values. Never commit real credentials here.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=<your-anon-key>
